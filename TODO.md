# TODO - Supabase/Prisma Auth Connection

- [x] Implement login via Prisma DB (UserInfo.name + password) replacing hardcoded USERS in `src/lib/auth.ts`

- [x] Ensure JWT cookie behavior remains same
- [x] Build and run checks: `pnpm build` (and `pnpm dev` if needed)
